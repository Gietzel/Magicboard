# Magicboard
MagicBoard is an Android drawing application designed to function as a deceptive tool for performing prediction-style magic tricks. At first glance, the app behaves like a simple sketch pad, allowing the user to freely draw on the screen using multiple colors, erase strokes, and clear the canvas. 
